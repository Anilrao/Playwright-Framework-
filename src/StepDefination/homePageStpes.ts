import { Given, Then } from "@cucumber/cucumber";

Given('the user is on login Page', async function () {
    // Navigate to login page
});

Given('the user enters the Valid username and password', async function () {
    // Enter username and password
});

Given('the user click on Login button', async function () {
    // Click login button
});

Then('the user should be successfully navigated to the home page', async function () {
    // Verify home page is displayed
});